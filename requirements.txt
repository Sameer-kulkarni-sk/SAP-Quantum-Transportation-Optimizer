# SAP Quantum Transport Optimizer — dependencies
# Install: pip install -r requirements.txt

# Qiskit 2.x (no qiskit-optimization or qiskit-algorithms needed)
qiskit>=2.0.0
qiskit-aer>=0.15.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# GUI
pillow>=10.0.0
# tkinter ships with Python — no pip install required
